plugins {
    id 'application'
}

repositories {
    mavenCentral()
}

test {
    beforeTest { descriptor ->
        logger.lifecycle("Running test: ${descriptor}")
    }
}

application {
    mainClass = 'de.viadee.javakurs.App'
}

dependencies {
}
